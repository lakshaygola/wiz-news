export const NoNews = () => {
    return (
        <div className="flex flex-col items-center justify-center w-screen h-screen">
            <div>No latest news</div>
            <div>Try after some time!!</div>
        </div>
    );
}